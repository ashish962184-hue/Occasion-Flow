# Backend Connection Report

## Phase 1: Backend Discovery

- **Backend Location**: `/backend` directory inside project.
- **Backend URL**: Defined in `.env.production` as `https://modern-bags-hear.loca.lt`
- **Health Endpoint**: Found at `GET /api/health`
- **API Structure**: Express router with endpoints for `/customers`, `/occasions`, `/orders`, `/dashboard`, `/reports`, etc.

The backend is structured properly as a separate Node/Express service.
